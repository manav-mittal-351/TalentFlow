# TalentFlow Frontend Environment Variables Template
# Copy this file to '.env' and modify as necessary

# Central Backend REST API endpoint base URL (must start with VITE_ to be exposed to client bundle)
VITE_API_BASE_URL=http://localhost:5000/api/v1
